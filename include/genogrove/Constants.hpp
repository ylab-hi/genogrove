#ifndef GENOGROVE_CONSTANTS_HPP
#define GENOGROVE_CONSTANTS_HPP

// Standard
#include <cstddef>

namespace genogrove {
    namespace constants {
        static constexpr size_t BITS = 6; // 2^6 = 64 chromosomes (should be enough for now...)
    }
}

#endif //GENOGROVE_CONSTANTS_HPP
